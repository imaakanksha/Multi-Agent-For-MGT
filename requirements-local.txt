# ============================================================
# Minimal dependencies for zero-cost local deployment
# (Ollama + SQLite — no Azure, no paid APIs)
# ============================================================
# Install with: pip install -r requirements-local.txt
# ============================================================

# Core orchestration
langgraph>=0.3.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.3.0

# Data models & validation
pydantic>=2.9.0

# API server
fastapi>=0.115.0
uvicorn>=0.32.0

# Environment & config
python-dotenv>=1.0.0

# Testing
pytest>=8.3.0
